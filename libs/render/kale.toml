[package]
name = "kale-render"
version = "0.1.0"
description = "GPU batching, font atlas caching, and shader pipeline renderer for Kale"
authors = ["Tomlin7 <billydevbusiness@gmail.com>"]
license = "MIT"
edition = "2026"
entry = "batch.kl"

[dependencies]
std = { path = "../../packages/std" }
gl = { path = "../gl" }
