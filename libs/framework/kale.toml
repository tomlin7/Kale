[package]
name = "kale-framework"
version = "0.1.0"
description = "Application lifecycle, unified event polling, and GLFW window coordinator for Kale"
authors = ["Tom Lin <tom@kale-lang.org>"]
license = "MIT"
edition = "2026"
entry = "app.kl"

[dependencies]
std = { path = "../../packages/std" }
