[package]
name = "kale-sql"
version = "0.1.0"
description = "Embedded SQL engine, B-Tree indexing, and relational query processor for Kale"
authors = ["Tom Lin <tom@kale-lang.org>"]
license = "MIT"
edition = "2026"
entry = "sql.kl"

[dependencies]
std = { path = "../../packages/std" }
