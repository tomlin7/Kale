[package]
name = "kale-term"
version = "0.1.0"
description = "Terminal emulator buffer, ANSI escape parser, and shell process bridge for Kale"
authors = ["Tom Lin <tom@kale-lang.org>"]
license = "MIT"
edition = "2026"
entry = "term.kl"

[dependencies]
std = { path = "../../packages/std" }
