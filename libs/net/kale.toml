[package]
name = "kale-net"
version = "0.1.0"
description = "Networking library, TCP/UDP sockets, HTTP client, and URL parser for Kale"
authors = ["Tom Lin <tom@kale-lang.org>"]
license = "MIT"
edition = "2026"
entry = "http.kl"

[dependencies]
std = { path = "../../packages/std" }
