[package]
name = "kale-std"
version = "1.0.0"
description = "Core standard library for the Kale programming language"
authors = ["Tom Lin <tom@kale-lang.org>"]
license = "MIT"
edition = "2026"
entry = "std.kl"

[dependencies]

[scripts]
test = "uv run pytest tests/test_std_*.py"
